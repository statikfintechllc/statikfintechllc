name: Update zsh.installer Card

on:
  schedule:
    - cron: "0 */23 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install deps
        working-directory: docs/Z.I.svg
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Build zsh.installer SVG
        env:
          PAT_GITHUB: ${{ secrets.PAT_GITHUB }}
        working-directory: docs/Z.I.svg
        run: node scripts/generate-zsh-installer.mjs

      - name: Commit and push
        run: |
          git config --global user.name "gremlin-bot"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "chore(Z.I.svg): update zsh.installer repo badge"
          git pull --rebase
          git push


